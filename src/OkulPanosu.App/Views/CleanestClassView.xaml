<UserControl x:Class="OkulPanosu.App.Views.CleanestClassView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24" Background="{StaticResource BgPrimaryBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="🧹 Haftanın En Temiz Sınıfları (Hijyen Ödülü)" FontSize="20" FontWeight="Bold" Foreground="White" VerticalAlignment="Center" />
            <Button Grid.Column="1" Content="+ Yeni Sınıf Ekle" Style="{StaticResource PrimaryButtonStyle}" Click="AddEntry_Click" />
        </Grid>

        <TextBlock Grid.Row="1" x:Name="StatusText" Foreground="{StaticResource SuccessBrush}" Margin="0,10,0,0"
                   TextWrapping="Wrap" Visibility="Collapsed" />

        <StackPanel Grid.Row="2" Margin="0,16,0,0">
            <ItemsControl x:Name="EntriesList">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="3" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
            <Button x:Name="SaveButton" Content="Değişiklikleri Kaydet" Style="{StaticResource PrimaryButtonStyle}"
                    HorizontalAlignment="Left" Margin="0,14,0,0" Click="Save_Click" />
        </StackPanel>
    </Grid>
</UserControl>
