<UserControl x:Class="OkulPanosu.App.Views.Modules.ClockDateModuleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Padding="14">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock x:Name="TimeText" FontSize="48" FontWeight="Bold" HorizontalAlignment="Center" />
            <TextBlock x:Name="DateText" FontSize="18" HorizontalAlignment="Center" Margin="0,6,0,0"
                       Style="{StaticResource MutedTextStyle}" />
            <StackPanel x:Name="WeatherPanel" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0"
                        Visibility="Collapsed">
                <TextBlock x:Name="WeatherIcon" FontSize="26" VerticalAlignment="Center" Margin="0,0,8,0" />
                <TextBlock x:Name="WeatherText" FontSize="15" VerticalAlignment="Center" />
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
