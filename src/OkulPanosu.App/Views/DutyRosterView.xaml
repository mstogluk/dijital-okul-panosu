<UserControl x:Class="OkulPanosu.App.Views.DutyRosterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24" Background="{StaticResource BgPrimaryBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="🗓️ Haftalık Kat Nöbetçi Çizelgesi" FontSize="20" FontWeight="Bold" Foreground="White" VerticalAlignment="Center" />
            <Button x:Name="SaveButton" Grid.Column="1" Content="Kaydet" Style="{StaticResource PrimaryButtonStyle}" Click="Save_Click" />
        </Grid>

        <TextBlock Grid.Row="1" Style="{StaticResource MutedTextStyle}" TextWrapping="Wrap">
            💡 İpucu: Açılır listelerden birine tıklayıp bir harfe basarsanız, o harfle başlayan isme
            hızlıca atlarsınız — uzun listelerde her seferinde aşağı kaydırmanıza gerek kalmaz.
        </TextBlock>

        <TextBlock Grid.Row="2" x:Name="StatusText" Foreground="{StaticResource SuccessBrush}" Margin="0,10,0,0"
                   TextWrapping="Wrap" Visibility="Collapsed" />

        <Border Grid.Row="3" Style="{StaticResource CardStyle}" Margin="0,16,0,0">
            <StackPanel>
                <TextBlock Text="Excel'den/İdareden Gelen Çizelgeyi Yapıştır" Style="{StaticResource HeadingStyle}" Margin="0,0,0,4" />
                <TextBlock TextWrapping="Wrap" Style="{StaticResource MutedTextStyle}" Margin="0,0,0,8">
                    İdareden gelen çizelgeyi Excel'e aktarıp oradan kopyalayarak (veya doğrudan) buraya
                    yapıştırın — HANGİ YÖNDE olduğu (satırlar gün/sütunlar nöbet yeri, ya da tam tersi)
                    otomatik anlaşılır, siz karar vermenize gerek yok. Sütun/satır başlıkları "Bahçe",
                    "Zemin Kat", "1. Kat" .. gibi ya da "Nöbetçi Müdür Yrd." olmalı; aynı yerde birden fazla
                    kişi varsa o satır/sütun tekrarlanabilir ya da hücrede (Alt+Enter ile) ayrı satırda
                    yazılabilir. Boş hücreler "-" ile bırakılabilir. Kısaltılmış isimler ("E.BAKIR" gibi)
                    Personel listesindeki tam adla eşleştirilmeye çalışılır — eşleşmezse yazdığınız gibi
                    kaydedilir.
                </TextBlock>
                <TextBox x:Name="PasteBox" Style="{StaticResource WatermarkTextBoxStyle}" AcceptsReturn="True"
                         AcceptsTab="True" TextWrapping="NoWrap" Height="110"
                         FontFamily="Consolas" FontSize="12"
                         VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" />
                <Button Content="Yapıştırılanı Uygula" Style="{StaticResource SecondaryButtonStyle}"
                        HorizontalAlignment="Left" Margin="0,8,0,0" Click="ApplyPaste_Click" />
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="4" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Margin="0,16,0,0">
            <Grid x:Name="MatrixGrid" />
        </ScrollViewer>
    </Grid>
</UserControl>
