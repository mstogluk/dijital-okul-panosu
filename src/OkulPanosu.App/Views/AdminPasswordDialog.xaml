<Window x:Class="OkulPanosu.App.Views.AdminPasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Yönetici Şifresi" Icon="/Assets/AppIcon.ico" Height="180" Width="360"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False" Topmost="True" Background="{StaticResource BgPrimaryBrush}">
    <Grid Margin="18" Background="{StaticResource BgPrimaryBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Bu işlem yönetici şifresi gerektiriyor" Style="{StaticResource HeadingStyle}" Margin="0,0,0,12" />

        <PasswordBox Grid.Row="1" x:Name="PasswordInput" Style="{StaticResource WatermarkPasswordBoxStyle}"
                     KeyDown="PasswordInput_KeyDown" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="İptal" Style="{StaticResource SecondaryButtonStyle}" Width="80" Margin="0,0,8,0" Click="Cancel_Click" />
            <Button Content="Onayla" Style="{StaticResource PrimaryButtonStyle}" Width="80" Click="Confirm_Click" IsDefault="True" />
        </StackPanel>
    </Grid>
</Window>
