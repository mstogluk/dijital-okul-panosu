<UserControl x:Class="OkulPanosu.App.Views.FoodMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24" Background="{StaticResource BgPrimaryBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="🍽️ Haftalık Menü Yemek Giriş Matrisi" FontSize="20" FontWeight="Bold" Foreground="White" VerticalAlignment="Center" />
            <Button x:Name="SaveButton" Grid.Column="1" Content="Kaydet" Style="{StaticResource PrimaryButtonStyle}" Click="Save_Click" />
        </Grid>
        <TextBlock Grid.Row="0" Text="Her gün için 4 çeşit yemek ve kalori değeri girilebilir." Style="{StaticResource MutedTextStyle}"
                   Margin="0,32,0,0" VerticalAlignment="Bottom" />

        <TextBlock Grid.Row="1" x:Name="StatusText" Foreground="{StaticResource SuccessBrush}" Margin="0,10,0,0"
                   TextWrapping="Wrap" Visibility="Collapsed" />

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="0,16,0,0">
            <ItemsControl x:Name="DaysList">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="5" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
