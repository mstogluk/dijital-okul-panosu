<UserControl x:Class="OkulPanosu.App.Views.StudentOfMonthView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24" Background="{StaticResource BgPrimaryBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="🏆 Ayın Örnek Öğrencisi" FontSize="20" FontWeight="Bold" Foreground="White" VerticalAlignment="Center" />
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="+ Öğrenci Ekle" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,8,0" Click="AddStudent_Click" />
                <Button x:Name="SaveButton" Content="Kaydet" Style="{StaticResource SecondaryButtonStyle}" Click="Save_Click" />
            </StackPanel>
        </Grid>
        <TextBlock Grid.Row="0" Text="Her ay için bir öğrenci ekleyin — sadece &quot;Yayında&quot; işaretli olanlar panoda sırayla gösterilir."
                   Style="{StaticResource MutedTextStyle}" Margin="0,32,0,0" VerticalAlignment="Bottom" />

        <TextBlock Grid.Row="1" x:Name="StatusText" Foreground="{StaticResource SuccessBrush}" Margin="0,10,0,0"
                   TextWrapping="Wrap" Visibility="Collapsed" />

        <Grid Grid.Row="2" Margin="8,16,8,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="130" />
                <ColumnDefinition Width="1.1*" />
                <ColumnDefinition Width="0.5*" />
                <ColumnDefinition Width="1.6*" />
                <ColumnDefinition Width="1.2*" />
                <ColumnDefinition Width="70" />
                <ColumnDefinition Width="190" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="1" Text="AD SOYAD" Style="{StaticResource MutedTextStyle}" FontWeight="Bold" FontSize="11" />
            <TextBlock Grid.Column="2" Text="SINIF" Style="{StaticResource MutedTextStyle}" FontWeight="Bold" FontSize="11" />
            <TextBlock Grid.Column="3" Text="SEÇİLME GEREKÇESİ" Style="{StaticResource MutedTextStyle}" FontWeight="Bold" FontSize="11" />
            <TextBlock Grid.Column="4" Text="SÖZ (OPSİYONEL)" Style="{StaticResource MutedTextStyle}" FontWeight="Bold" FontSize="11" />
            <TextBlock Grid.Column="5" Text="YAYINDA" Style="{StaticResource MutedTextStyle}" FontWeight="Bold" FontSize="11" />
            <TextBlock Grid.Column="6" Text="İŞLEMLER" Style="{StaticResource MutedTextStyle}" FontWeight="Bold" FontSize="11" />
        </Grid>

        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="StudentsList" />
        </ScrollViewer>
    </Grid>
</UserControl>
